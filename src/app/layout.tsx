import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Shamel Enterprise Pricing Calculator | Healthcare Benefits by Vezeeta",
  description:
    "Calculate your enterprise healthcare costs with Shamel by Vezeeta. Compare TCO and ROI vs traditional insurance in Egypt.",
  openGraph: {
    title: "Shamel Enterprise Pricing Calculator",
    description: "Compare Shamel healthcare benefits vs traditional insurance for your company",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
